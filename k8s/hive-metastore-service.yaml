apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert --out k8s/
    kompose.version: 1.35.0 (HEAD)
  labels:
    io.kompose.service: hive-metastore
  name: hive-metastore
spec:
  ports:
    - name: "9083"
      port: 9083
      targetPort: 8000
  selector:
    io.kompose.service: hive-metastore
